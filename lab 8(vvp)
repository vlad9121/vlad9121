#include <math.h>
#include <iostream>

using namespace std;

int main()
{

    cout <<"Задача 1" << '\n';
    int A, B;
    cout << "Введите значение A = ";
    cin >> A;
       cout << "Введите значение B = ";
    cin >> B;
    if (A == B) {
        A = 0;
        B = 0;
        cout << "Значение  A = " << A << '\n';
        cout << "Значение  B = " << B << '\n';
    }
    else if (A > B) {
        B = A;
        cout << "Значение  A = " << A << '\n';
        cout << "Значение  B = " << B << '\n';
    }
    else {
        A = B;
          cout << "Значение  A = " << A << '\n';
        cout << "Значение  B = " << B << '\n';
    }
    cout <<"Задача 2" << '\n';
    int A1, B1, C1, S;
    cout << "Введите значение A = ";
    cin >> A1;
    cout << "Введите значение B = ";
    cin >> B1;
    cout << "Введите значение C = ";
    cin >> C1;
    if (A1 > B1) {
        if (B1>C1) {
            S= A1+B1;
            cout << "Значение  S = " << S << '\n';
        }
     else  {
         S = A1+C1;
            cout << "Значение  S = " << S << '\n';
     }
    }
     else if (C1 > A1) {
            S= C1+B1;
            cout << "Значение  S = " << S << '\n';
     }
     else {
         S = B1 + A1;
            cout << "Значение  S = " << S << '\n';
     }  


      cout <<"Задача 3" << '\n';
double Xa, Xb, Xc, Ya, Yb, Yc, R, AB, AC;
    cout << "Введите координату Xa = ";
    cin >> Xa;
    cout << "Введите координату Ya = ";
    cin >> Ya;
    cout << "Введите координату Xb = ";
    cin >> Xb;
    cout << "Введите координату Yb = ";
    cin >> Yb;
    cout << "Введите координату Xc = ";
    cin >> Xc;
    cout << "Введите координату Yc = ";
    cin >> Yc;
    AB = sqrt((Xb-Xa)*(Xb-Xa)+(Yb-Ya)*(Yb-Ya));
    AC = sqrt((Xc-Xa)*(Xc-Xa)+(Yc-Ya)*(Yc-Ya));
    if (AB < AC) {
        cout <<"Ближайшая к А точка B, расстояние между ними: " << AB << '\n';

    }
    else {
        cout <<"Ближайшая к А точка C, расстояние между ними: " << AC << '\n';

    }
     cout <<"Задача 4" << '\n';
double Y1, X1;
    cout << "Введите координату Xa = ";
    cin >> X1;
    cout << "Введите координату Ya = ";
    cin >> Y1;
    if (Y1 > 0 && X1 > 0) {
        cout << "Точка лежит в 1 четверти" << '\n';}
    if (Y1 > 0 && X1 < 0) {
        cout << "Точка лежит в 2 четверти" << '\n';}
    if (Y1 < 0 && X1 < 0) {
        cout << "Точка лежит в 3 четверти" << '\n';}
    if (Y1 < 0 && X1 > 0) {
        cout << "Точка лежит в 4 четверти" << '\n';}
     cout <<"Задача 5" << '\n';
    int P;
    cout << "Введите число P = ";
    cin >> P;
    if (P < 0) {
        if ((P % 2) == 0) {
            cout << " P - отрицательное, чётное число" << '\n';
        }
        else {
            cout << " P - отрицательное, нечётное число" << '\n';
        }
         }
         else if (P > 0) {
        if ((P % 2) == 0) {
            cout << " P - положительное, чётное число" << '\n';
        }
        else {
            cout << " P - положительное, нечётное число" << '\n';
        }
         }
         else { cout << "P - нулевое число" << '\n';
         }
          cout <<"Задача 6" << '\n';
          int O;
          cout << "Введите число O = ";
          cin >> O;
          if ((O / 10) == 0) {
              if ((O % 2) == 0) {
                   cout << " P - однозначное, чётное число" << '\n';
                     }
             else {
                  cout << " P - однозначное, нечётное число" << '\n';

             }
          }
         else if ((O / 10) >= 1 && (O / 10) <= 9 ) {
              if ((O % 2) == 0) {
                   cout << " P - двузначное, чётное число" << '\n';
                     }
             else {
                  cout << " P - двузначное, нечётное число" << '\n';

             }
          }
           else if ((O / 10) >= 10 && (O / 10) <= 99) {
              if ((O % 2) == 0) {
                   cout << " P - трёхзначное, чётное число" << '\n';
                     }
             else {
                  cout << " P - трёхзначное, нечётное число" << '\n';

             }
          }
          return 0;
             }
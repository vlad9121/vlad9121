#include <iostream>
#include <string>
#include <math.h>
#include <stdio.h>
using namespace std; 



int main()
{
    setlocale(0, " ");
    float a, X, A, z, y, b, V1, V2, S, T, L, B, A1, B1, C1, A2, B2, C2, J, I, x;
    cout<<"Лабораторная 4"<< '\n';
    cout << "Задача 1 "<< '\n';
    cout << "Введите значение угла a в градусах, от 0 до 360 градусов, a = ";
    cin >> a;
    if (a > 0 && a < 360 ) {  
   a = a/180;
    cout << " Угол a = " << a << "П " << endl;
} else {  
    cout << "Вы ввели неправильное значение a" << endl;
}
 cout << "Задача 2 "<< '\n';
 cout << " Введите значение угла в радианах, П, a = ";
 cin >> a;
 if (a > 0 && a < 2 ) {  
   a = a*180;
    cout << " Угол a = " << a << " градусов " << endl;
} else {  
    cout << "Вы ввели неправильное значение a" << endl;
}
cout << "Задача 3 "<< '\n';
cout << " Введите количество конфет, в кг, X = ";
 cin >> X;
 cout << " Введите стоимость " <<  X << " кг конфет: ";
 cin >> A;
 z = A/X;
 cout << " Введите нужное количество кг конфет: ";
 cin >> y;
 b = z*y;
   cout << "Стоимость одного кг: " << z << " рублей " << endl;
    cout << "Стоимость " << y << " кг: " << b << " рублей " << endl;
    cout << "Задача 4 "<< '\n';
    cout << "Введите скорость первого автомобиля в км/ч, V1 = ";
    cin >> V1;
    cout << "Введите скорость второго автомобиля в км/ч, V2 = ";
    cin >> V2;
    cout << "Введите расстояние между автомобилями в км,  S = ";
    cin >> S;
    cout << "Введите время движения автомобилей,  T = ";
    cin >> T;
    L = S + V1 * T + V2 * T;
    cout << "Расстояние между ними будет равно " << L << " км " << endl;
    cout << "Задача 5 "<< '\n';
    cout << "Дано уравнение Ax+B=0 \n ";
    cout << "Введите значение A = ";
    cin >> A;
     if (A > 0 || A < 0 ) {  
    cout << "Введите значение B = ";
    cin >> B;     
   X = (0 - B) / A ;
    cout << " X = " << X << endl;
} else {  
    cout << "Вы ввели нулевое значение A " << endl;
}
    cout << "Задача 6 "<< '\n';
      cout << "Введите значение A1 = ";
    cin >> A1;
      cout << "Введите значение B1 = ";
    cin >> B1;
      cout << "Введите значение C1 = ";
    cin >> C1;
      cout << "Введите значение A2= ";
    cin >> A2;
      cout << "Введите значение B2 = ";
    cin >>  B2;
      cout << "Введите значение C2 = ";
    cin >> C2;

    y = ((C1+A1*C2)/A2)/(B1-((A1*B2)/A2));
    x = (C2-(B2*y))/A2;
    cout << "x = " << x << endl;
    cout << "y = " << y << endl;



    return 0;
}